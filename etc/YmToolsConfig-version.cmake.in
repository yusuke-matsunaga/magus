set (PACKAGE_VERSION "@YMTOOLS_VERSION@")
if (NOT "${PACKAGE_FIND_VERSION}" VERSION_GREATER "@YMTOOLS_VERSION@")
  set (PACKAGE_VERSION_COMPATIBLE 1)
  if ("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@YMTOOLS_VERSION@")
    set (PACKAGE_VERSION_EXACT 1)
  endif ()
endif ()